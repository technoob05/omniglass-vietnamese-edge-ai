numpy==1.26.4
opencv-python==4.10.0.84
supervision==0.26.1
transformers==4.43.4
gradio==5.49.1
requests>=2.31,<3

# Install CUDA-matched torch/torchvision first, then install the upstreams:
# SAM2_BUILD_CUDA=0 pip install -e upstream/Grounded-SAM-2
# CUDA_HOME=/usr/local/cuda pip install --no-build-isolation -e upstream/Grounded-SAM-2/grounding_dino
